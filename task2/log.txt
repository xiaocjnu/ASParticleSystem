3/27
1. 重构canvas模式，所有发射器统一在一张位图(static的）下绘制。
   位图addChild到了stage中。
   引起的问题：
       将发射器addChild到其他容器中后，绘制的图片不会跟随容器运动。（位置只与发射器的x,y坐标有关）
   目前可解决方法：
       1）有这种需求的用particle模式渲染。
       2）用ASFollow类，让发射器跟随容器运动。